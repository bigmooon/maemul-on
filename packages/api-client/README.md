# @maemul-on/api-client

FastAPI의 OpenAPI 스키마에서 **생성**하는 TypeScript 클라이언트.

- 이 디렉터리의 생성 결과물을 손으로 고치지 않는다. 스키마를 고치고 다시 생성한다.
- 스키마가 바뀌면 `make api-client`로 재생성하고 **diff를 검토**한다 (완료의 정의).

**현재 비어 있다.** 생성할 제품 API가 아직 없다 (TASK-0002).
